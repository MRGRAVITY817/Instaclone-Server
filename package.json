{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm run ts:build & nodemon --exec node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "ts:build": "npx tsc -w",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server": "^2.21.1",
    "graphql": "^15.5.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "prisma": "^2.18.0",
    "tslint": "^6.1.3",
    "typescript": "^4.0.3",
    "gts": "^3.1.0",
    "@types/node": "^14.11.2"
  }
}
